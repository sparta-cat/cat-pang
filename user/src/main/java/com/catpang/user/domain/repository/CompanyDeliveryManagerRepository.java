package com.catpang.user.domain.repository;

import com.catpang.user.domain.model.CompanyDeliveryManager;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CompanyDeliveryManagerRepository extends JpaRepository<CompanyDeliveryManager, Long> {
}
