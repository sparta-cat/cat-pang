eureka:
  client:
    service-url:
      defaultZone: http://localhost:19090/eureka/
    fetch-registry: 'false'
    register-with-eureka: 'false'
  instance:
    hostname: localhost
spring:
  application:
    name: server
server:
  port: '19090'
