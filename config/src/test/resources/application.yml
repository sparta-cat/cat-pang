server:
  port: 18080

spring:
  profiles:
    active: native
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config-repo  #리소스 폴더 디렉토리

# 로그 수준
logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace